<template>
  <pre class="demo-loading">กำลังโหลด...</pre>
</template>

<style scoped>
.demo-loading {
  color: var(--ink-soft);
  font-family: var(--font-mono);
  font-size: 0.8rem;
  white-space: pre-wrap;
  padding: var(--space-3);
  border: 1px dashed var(--line-strong);
  border-radius: var(--radius-sm);
}
</style>
