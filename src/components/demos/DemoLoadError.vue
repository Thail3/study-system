<template>
  <pre class="demo-load-error">โหลด component ไม่สำเร็จ ลองรีเฟรชหน้านี้อีกครั้ง</pre>
</template>

<style scoped>
.demo-load-error {
  color: var(--mark-red);
  font-family: var(--font-mono);
  font-size: 0.8rem;
  white-space: pre-wrap;
  padding: var(--space-3);
  border: 1px dashed var(--mark-red);
  border-radius: var(--radius-sm);
}
</style>
